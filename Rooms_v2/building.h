#ifndef BUILDING_H
#define BUILDING_H
#include "room.h"
#include "fileops.h"
#include <cmath>
/*
class Building {

private:
    std::string bName;
    std::vector<Room> rooms;
    int nRooms();
    int totalArea();

public:
    Building(std::string bName, std::vector<Room> rooms);

    int cleaners(int nRooms, int totalArea){

        int krav1 = std::ceil(totalArea / 15);

        int krav2 = std::ceil(nRooms / 2);

        return std::max(krav1, krav2);
    };


};
*/
#endif // BUILDING_H
