#ifndef BUILDING_H
#define BUILDING_H
#include <vector>
#include <string>
#include "room.h"

class Building {
public:
//     std::string bName;               // Name of the building
//     std::vector<Room> rooms;        // Vector that stores rooms
//    int cleaners();                     // Number of cleaners required
//     int buildingArea();             // Total area of all rooms in the building

//     Building(std::string bName) : bName(bName) {}

//     void addRoom(const Room& room){
//         rooms.push_back(room);
//     }
};

#endif // BUILDING_H
